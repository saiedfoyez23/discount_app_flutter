import 'package:flutter/material.dart';

class VendorProductEditView extends StatelessWidget {
  const VendorProductEditView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
